## 1. 测试基础设施与迁移准备

先改造测试工具，使后续每一项都能写出真正会失败的测试；再补齐 schema，避免后续阶段反复改表。

- [x] 1.1 在 `smoke-test.mjs` 增加 `sendRawUpdate(env, update)`，直接投递调用方构造的完整 update，`update_id` 与 `message_id` 完全由测试控制
- [x] 1.2 把故障注入升级为可编程钩子：支持按方法、按第 N 次调用返回指定 `error_code` 与 `description`，覆盖 400/403/429/5xx 与网络异常
- [x] 1.3 增加可控延迟钩子：允许测试挂起某次 Telegram 调用直到显式放行，用于构造迟到响应与占位接管
- [x] 1.4 实现幂等 schema 迁移：为 `processed_updates` 增加 `status`、`lease_until`、`claim_token`，为 `users` 增加 `topic_claim_token`，为 `config` 增加 `version`；已有 `processed_updates` 行按 `done` 解释
- [x] 1.5 补测试：迁移在已有数据的库上重复执行不报错，且既有用户、话题映射与配置值保持不变
- [x] 1.6 跑全量冒烟测试，确认既有断言无回归

## 2. update claim 与租约（Critical，其余阶段的基础）

- [x] 2.1 写先失败的测试：用 `sendRawUpdate` 并发投递同一 `update_id`，断言成功的 `forwardMessage` 恰为 1 次
- [x] 2.2 写先失败的测试：某 update 处理中断后，同一 `update_id` 重投仍能被处理并最终送达
- [x] 2.3 实现 claim + 租约 + 终态：执行任何副作用前原子取得处理权，成功后才写 `status='done'`
- [x] 2.4 未取得处理权（他人租约未过期）时返回 500 交重投，不执行任何副作用
- [x] 2.5 租约过期允许接管，确保 Worker 崩溃不会使消息永久卡死
- [x] 2.6 标定租约时长：结合 `tg()` 总等待上限与实测处理时长确定取值，并在常量注释中写明依据
- [x] 2.7 跑全量冒烟测试，确认 2.1/2.2 转为通过且既有断言无回归

## 3. 限流与重投解耦（Critical）

- [x] 3.1 写先失败的测试：用户在同一限流窗口内发满额度，其中一条因未完成返回 500，用同一 `update_id` 重投，断言该消息最终送达而非被限流吞掉
- [x] 3.2 将限流决策与 update 身份绑定：首次 claim 时计量一次，重投复用已记录的决策
- [x] 3.3 确认限流提示防抖行为不受本次改动影响
- [x] 3.4 跑全量冒烟测试，确认 3.1 转为通过且既有限流断言无回归

## 4. Telegram 错误分类（Critical，话题与回复修复的前置）

- [x] 4.1 实现统一分类函数，输出 `topic_gone`、`topic_closed`、`permanent`、`transient` 四类，覆盖 429、5xx、网络异常与永久 4xx
- [x] 4.2 让需要分类的调用不再经过会把错误折叠为 `null` 的静默包装，保留 `error_code` 与描述
- [x] 4.3 写先失败的测试：话题被关闭且 `reopenForumTopic` 持续返回 5xx，断言话题映射未被清空且本次请求返回 500
- [x] 4.4 修改话题关闭恢复路径：仅当重开失败且为永久性错误时才按删除处理，临时失败保持映射并请求重投
- [x] 4.5 写先失败的测试：管理员在话题内回复，`copyMessage` 持续 5xx，断言返回 500 请求重投
- [x] 4.6 修改管理员回复路径：永久失败在话题内提示并返回 200，临时失败抛出重投
- [x] 4.7 跑全量冒烟测试，确认既有「临时错误不清映射」「用户停用机器人」断言无回归

## 5. 话题所有权 fencing（High）

- [x] 5.1 写先失败的测试：占位过期被接管后原创建请求才返回，断言最终映射为接管者创建的话题
- [x] 5.2 写先失败的测试：针对旧话题的迟到失效错误在新映射写入后返回，断言新映射未被清空
- [x] 5.3 实现用户话题 claim 的 fencing token：写回映射时校验自身仍为占位持有者
- [x] 5.4 将失效映射清除改为条件更新，仅当当前映射仍为本次失败所针对的话题时才清除
- [x] 5.5 写回失败时尝试删除刚创建的孤儿话题，失败仅记录日志，不影响主流程
- [x] 5.6 黑名单共享话题采用同构方案：占位带 token，释放时校验归属，避免解除他人占位
- [x] 5.7 跑全量冒烟测试，确认并发建话题与话题重建相关断言无回归

## 6. 编辑消息准入（High）

- [x] 6.1 写先失败的测试：用户被 `/reset` 后编辑旧消息，断言编辑内容未进入管理群且用户收到验证引导
- [x] 6.2 写先失败的测试：把已发送消息编辑为含屏蔽关键字的内容，断言不转达并按屏蔽策略处理
- [x] 6.3 提取统一准入管线（屏蔽 → 限流 → 验证状态 → 关键词 → 类型过滤）为共享函数
- [x] 6.4 让编辑消息路径复用该管线，通过后才发送编辑提示
- [x] 6.5 跑全量冒烟测试，确认已验证用户的正常编辑通知仍然工作

## 7. 资料卡管理入口（High）

- [x] 7.1 写先失败的测试：话题创建后不做任何其他操作，断言初始置顶资料卡的 `reply_markup` 含屏蔽与备注按钮
- [x] 7.2 让首次发送的资料卡携带与刷新后一致的操作按钮
- [x] 7.3 补测试：直接点击初始资料卡上的屏蔽按钮可成功屏蔽用户
- [x] 7.4 跑全量冒烟测试，确认资料卡置顶与刷新断言无回归

## 8. 安全匹配子集（High）

- [x] 8.1 写先失败的测试：`re:` 配置 `a?` 量词链，断言视为不匹配且匹配耗时无异常增长
- [x] 8.2 写先失败的测试：`re:(?:a|aa|\))+$` 这类转义括号模式，断言被拒绝且耗时无异常增长
- [x] 8.3 实现安全子集解析器：按字符扫描并正确处理转义，接受字面量、字符类、常用转义、锚点及作用于单字符或字符类的量词
- [x] 8.4 拒绝分组、作用于分组的量词、嵌套量词、反向引用与变长后行断言，并将被拒条目视为不匹配且不报错
- [x] 8.5 保留模式长度与被检文本长度上限作为二次防线
- [x] 8.6 补测试：`re:加微信\d+`、`re:[Vv][Xx]\s*[:：]?\s*\w+`、`re:客服\s*QQ\s*\d{5,}` 等常规写法仍然生效
- [x] 8.7 在面板中标示被拒绝的 `re:` 条目为未生效，并补对应测试
- [x] 8.8 跑全量冒烟测试，确认既有关键词与自动回复断言无回归

## 9. 验证流程收敛（Medium）

- [x] 9.1 写先失败的测试：用户处于问答待答状态时管理员关闭问答但保留题库，断言用户发送普通消息后按剩余流程推进且不再收到题目
- [x] 9.2 让问答作答路径同样遵守问答开关，关闭时立即脱困
- [x] 9.3 写先失败的测试：同一有效 nonce 的两个提交并发到达，断言至多一个推进验证状态
- [x] 9.4 将 nonce 消费改为条件更新，未命中即视为已被消费
- [x] 9.5 写先失败的测试：同一用户并发触发验证入口下发，断言先前已下发且未过期的 nonce 仍可成功提交
- [x] 9.6 使 nonce 下发在并发下不互相作废
- [x] 9.7 跑全量冒烟测试，确认既有验证流程断言无回归

## 10. 欢迎语语义（Medium）

- [x] 10.1 写先失败的测试：新用户不发 `/start` 而直接发普通消息，断言收到欢迎语与验证引导
- [x] 10.2 让未验证用户的任意首条私聊消息都触发欢迎引导，且同一验证过程中不重复轰炸
- [x] 10.3 写先失败的测试：欢迎语保存为含 `<` `>` 的纯文本，断言发送成功且内容按原文呈现
- [x] 10.4 按纯文本语义转义欢迎语、媒体 caption 与替换后的 `{name}`
- [x] 10.5 写先失败的测试：媒体欢迎语发送失败时，断言用户不会收到包含媒体配置 JSON 的文本
- [x] 10.6 媒体失败改为发送转义后的 caption 或通用欢迎文案
- [x] 10.7 跑全量冒烟测试，确认既有欢迎语与面板预览断言无回归

## 11. 配置并发写入（Medium）

- [x] 11.1 写先失败的测试：两个管理员并发添加不同屏蔽词且都被确认保存，断言两者都存在于最终列表
- [x] 11.2 写先失败的测试：条目已被他人删除后再点击原删除按钮，断言不误删其他条目并提示未生效
- [x] 11.3 为配置写入实现基于 `version` 的 CAS，冲突时重新读取并重放本次意图，有限次重试
- [x] 11.4 为屏蔽词条目引入稳定删除键（内容哈希）并按该键定位删除，不改动存储结构
- [x] 11.5 确认屏蔽词结构对上一版本代码仍可读，满足回滚要求
- [x] 11.6 让开关切换基于当前持久化值原子完成，并补并发切换测试
- [x] 11.7 跑全量冒烟测试，确认既有面板增删改断言无回归

## 12. 规格一致性与收尾

- [x] 12.1 修正 `fix-verification-bypass-and-message-loss` 中要求 `esc()` 转义反斜杠的表述，使其与本变更「HTML 转义与 JS 字面量转义职责分离」一致
- [x] 12.2 复核 `README.md` 中受影响的说明：欢迎语纯文本语义、`re:` 安全子集限制与被拒条目提示
- [x] 12.3 复核全部新增注释是否解释了「为什么」，尤其 claim 租约取值、fencing 校验与安全子集的拒绝理由
- [x] 12.4 跑全量冒烟测试，确认全绿且新增断言全部通过
- [x] 12.5 向用户汇报：逐项说明修复方式与验证结果，明确列出行为变化（欢迎语不再解析 HTML、不属于安全子集的既有 `re:` 条目将失效）与已知残留边界
