# Tasks: fix-verification-bypass-and-message-loss

修复顺序按严重程度，每项独立可验证、可单独回滚。每完成一项立即跑 `node smoke-test.mjs`（基线 111 通过 / 0 失败），确认无回归再进行下一项。

## 1. 验证码绕过（Critical）

- [x] 1.1 为缺陷写**先失败**的冒烟测试：人机验证开启 + 问答关闭，用户 `/start` 后不点链接直接发第二条消息，断言 `state !== "verified"` 且消息未转发；确认该测试在修复前失败
- [x] 1.2 补一条同场景但问答开启的测试：断言用户不进入 `qa_pending`、未收到题目
- [x] 1.3 重构 `advanceVerification`：把"验证码已满足"从伪造 `state` 的隐式约定改为显式参数（如 `captchaSatisfied`），拆开 `new` 与 `captcha_pending` 的合并分支；`captcha_pending` 且验证码开启且未满足时重新下发验证入口
- [x] 1.4 更新两处调用点：`handleTokenSubmit`（worker.js:928/948）改为显式传递「已满足」并停止伪造 `state`；`handlePrivate`（worker.js:994）不传该标志
- [x] 1.5 验证「验证码关闭后待验证用户可脱困」路径未被回归破坏（原 `captcha_pending` 用户在管理员关闭验证码后应能推进）
- [x] 1.6 跑全量冒烟测试，确认 1.1/1.2 转为通过且既有 111 项无回归

## 2. 静默丢消息（Critical）

- [x] 2.1 修正 `smoke-test.mjs:422-431` 既有断言盲区：话题被删场景补充「捕获返回状态码」与「消息是否成功送达管理群」断言，确认其在修复前失败
- [x] 2.2 `forwardToTopic` 补齐 `handleTopicError` 返回 `"gone"` 的分支：清除映射后抛 `RetryLater`，交 Telegram 重投
- [x] 2.3 检查内层 `copyMessage` 失败路径（worker.js:1177-1181）的 `"gone"` 结果是否同样落空，一并处理
- [x] 2.4 补测试：话题被删后的消息经重投最终送达管理群，用户获得已送达标记
- [x] 2.5 确认 `RetryLater` 不会与 update 去重冲突——去重标记仅在成功后写入（worker.js:170），500 路径不写，重投可正常重试
- [x] 2.6 复核关联点 `sendToTopic`（worker.js:1195-1208）丢弃 `handleTopicError` 返回值的行为：承载的是资料卡/编辑提示等系统消息而非用户消息本体，判断是否需一并修复或明确记录为不修

## 3. 验证页 XSS（High）

- [x] 3.1 写先失败的测试：请求 `/verify?uid=%5C&nonce=%3BPWNED()%3B%2F%2F`，断言渲染出的 HTML 中注入代码未逃逸字符串上下文
- [x] 3.2 引入专用于 JS 字符串上下文的转义（`jsLiteral()`），`esc()` 维持纯 HTML 转义、不转义反斜杠——面板需按原文回显 `re:加微信\d+` 这类正则
- [x] 3.3 `handleVerifyPage` 的 JS 注入点（worker.js:865）改用 `JSON.stringify` 注入 `uid`/`nonce`，不再让 HTML 转义函数承担 JS 字符串转义职责
- [x] 3.4 回归确认 `esc()` 的其余调用方（资料卡、面板 `preview`、欢迎语）显示正常——尤其含 `re:加微信\d+` 的屏蔽词在面板中的回显
- [x] 3.5 补测试：`</script>` 向量与反斜杠向量均不产生可执行代码

## 4. ReDoS 防护绕过（Medium）

- [x] 4.1 写先失败的测试：屏蔽词配 `re:a*a*a*…b`（20 个量词），发送触发文本，断言匹配耗时无异常增长且视为不匹配
- [x] 4.2 `RE_REJECT` 增加量词总数上限规则，阈值取 6（实测标定：8 个常规管理员正则量词数上限为 3，4 个已知恶意样本下限为 8）
- [x] 4.3 补测试：`re:[Vv][Xx]\s*[:：]?\s*\w+`、`re:客服\s*QQ\s*\d{5,}` 等正常正则不被误伤
- [x] 4.4 确认新规则对既有测试中的 `re:加微信\d+`、`re:(a+)+$` 行为不变

## 5. 收尾

- [x] 5.1 跑全量冒烟测试，确认全绿且新增断言全部通过
- [x] 5.2 复核四处改动的注释是否解释了「为什么」（尤其 1.3 的显式参数设计意图、3.2 的替换顺序约束），符合项目注释规范
- [ ] 5.3 向用户汇报：逐项说明修复方式与验证结果，说明存量误验证用户不被追溯回收、需要时由管理员 `/reset` 处理
